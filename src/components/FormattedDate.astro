---
interface Props {
  date?: Date
}

const { date } = Astro.props;
const safeDate = date ?? new Date(); // fallback to current date
---

<time datetime={safeDate.toISOString()}>
  { safeDate.toLocaleDateString("en-uk") }
</time>