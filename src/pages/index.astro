---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_DESCRIPTION } from '../consts';
import RecentPosts from '../components/RecentPosts.astro';
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead title="The Galactic Gateway" description={SITE_DESCRIPTION} />
  </head>
  <script>
  	window.addEventListener("load", () => {
    	const loader = document.getElementById("nova-loader");
    	if (loader) {
      		loader.style.opacity = "0";
      		loader.style.transition = "opacity 0.5s ease";
      		setTimeout(() => loader.remove(), 500);
    	}
  	});
  </script>
  <body>
	<div id="comet-trail"></div>
	<div id="nova-loader">
  		<div class="nova-core"></div>
  		<div class="nova-trail"></div>
	</div>
    <Header />
    <main>
		<div class="nova-quote-box">
  			<p id="quote-text"></p>
  			<span id="quote-author" class="author"></span>
		</div>

		<RecentPosts />

		<section class="nova-section gateway-intro">
			<h2>What is The Galactic Gateway?</h2>
  			<div class="gateway-flex">
				<div class="gateway-text">
  					<p>
    					The Galactic Gateway is a space I built to share curiosity about the universe. The name comes from the feeling that astronomy is like a doorway — once you step through, you start seeing connections everywhere, from the stars above to the science shaping our future.
  					</p>
  					<p>
    					The blog mixes explanations of big cosmic ideas with smaller reflections, sometimes pulling in history, sometimes looking at the latest discoveries. It’s not meant to be a lecture hall, more like sitting down with someone who’s just as fascinated as you are and wants to swap thoughts.
  					</p>
					<p>
						At its heart, The Galactic Gateway is about keeping that sense of wonder alive. If you’ve ever looked up at the night sky and felt both tiny and amazed, you’re already in the right place.
					</p>
				</div>

				<div class="gateway-image">
					<img src="/GGFull.png" alt="The Galactic Gateway Illustration">
				</div>
			</div>
		</section>

		<section class="nova-section">
  			<h2>Meet Nova the Neutrino</h2>
  			<p>
    			Nova is your cosmic companion and resident explorer of The Galactic Gateway.
    			Tiny, speedy and endlessly curious, Nova zips through the universe in search of wonder,
    			weirdness and the occasional misplaced blog post.
  			</p>

  			<div class="nova-flex">
				<div class="nova-card">
  					<div class="nova-card-header">NEUTRINO PROFILE</div>
  					<table class="nova-table">
    					<tr><td><strong>Name</strong></td><td>Nova</td></tr>
    					<tr><td><strong>Species</strong></td><td>Neutrino</td></tr>
    					<tr><td><strong>Size</strong></td><td>Subatomic, emotionally large</td></tr>
    					<tr><td><strong>Speed</strong></td><td>Practically light-speed</td></tr>
    					<tr><td><strong>Mood</strong></td><td>90% wonder, 10% confusion</td></tr>
    					<tr><td><strong>Snack</strong></td><td>Cosmic microwave leftovers</td></tr>
    					<tr><td><strong>Hobbies</strong></td><td>Stargazing, particle surfing, photobombing quasars</td></tr>
  					</table>
  				</div>
				<div class="nova-img-wrapper">
					<img src="/Nova.png" alt="Nova the Neutrino" class-"nova-img" />
				</div>
			</div>

  			<p class="nova-lore">
				Nova is modeled after a neutrino—a fundamental particle with near-zero mass,
    			neutral charge, and the ability to pass through matter almost undetected.
    			They’re elusive, fast and rarely interact—but when they do, it’s usually something interesting.
    			Nova’s presence here reflects the idea that even the smallest particles can carry big curiosity.
				<br /><br />
				<em>Fun fact:</em> Trillions of neutrinos pass through your body every second, and you never notice.
  			</p>

  			<p class="nova-role">
				Nova helps guide visitors through The Galactic Gateway, appearing on 404 pages,
    			introducing blog posts and adding a spark of personality to your cosmic journey.
  			</p>
		</section>
    </main>
    <Footer />
	<script is:client>
  		async function loadQuote() {
    		try {
      			const response = await fetch('/data/quotes.json');
      			const quotes = await response.json();
      			const random = quotes[Math.floor(Math.random() * quotes.length)];

    			const textEl = document.getElementById('quote-text');
    			const authorEl = document.getElementById('quote-author');

				textEl.style.opacity = 0;
    			authorEl.style.opacity = 0;

				setTimeout(() => {
      				textEl.textContent = `“${random.text}”`;
      				authorEl.textContent = `— ${random.author}`;

      				textEl.style.opacity = 1;
      				authorEl.style.opacity = 1;
    			}, 600); 
  			} catch (err) {
    			console.error('Error loading quotes:', err);
  			}
		}

  		window.addEventListener('DOMContentLoaded', () => {
    		loadQuote();
    		setInterval(loadQuote, 5000);
  		});
	</script>
	<script is:inline>
		window.addEventListener("DOMContentLoaded", () => {
			document.addEventListener("mousemove", (e) => {
				const trail = document.getElementById("comet-trail");
				if (!trail) return;

				const particle = document.createElement("div");
				particle.classList.add("comet-particle");

				particle.style.left = e.clientX + "px";
				particle.style.top = e.clientY + "px";

				trail.appendChild(particle);

				setTimeout(() => {
					particle.remove();
				}, 400);
			});
		});
	</script>

  </body>
  <style>
  	@import url('https://fonts.googleapis.com/css2?family=Nunito:wght@400;700&display=swap');
  	#nova-loader {
    	position: fixed;
    	top: 0;
    	left: 0;
    	width: 100%;
    	height: 100%;
    	background: black;
    	display: flex;
    	align-items: center;
    	justify-content: center;
    	z-index: 9999;
  	}

  	.nova-core {
    	width: 40px;
    	height: 40px;
    	border-radius: 50%;
    	background: radial-gradient(circle at center, #8a2be2, #4b0082);
    	box-shadow: 0 0 20px #8a2be2, 0 0 40px #00ffff;
    	animation: pulse 1.5s infinite ease-in-out;
  	}

  	.nova-trail {
    	position: absolute;
 		width: 80px;
    	height: 80px;
    	border: 2px dashed rgba(138, 43, 226, 0.5);
    	border-radius: 50%;
    	animation: orbit 2s linear infinite;
  	}

  	@keyframes pulse {
    	0%, 100% { transform: scale(1); opacity: 1; }
    	50% { transform: scale(1.3); opacity: 0.7; }
  	}

  	@keyframes orbit {
    	0% { transform: rotate(0deg); }
    	100% { transform: rotate(360deg); }
  	}

	.nova-quote-box {
  		background: var(--section-bg);
  		padding: 0.5em;
  		border-radius: 12px;
  		text-align: center;
  		max-width: 700px;
  		margin: 1em auto;
		box-shadow: var(--shadow);
		color: var(--nova-box-text);
		border: 1px solid var(--border);
		font-family: 'Nunito', sans-serif;
		font-weight: 500;
		line-height: 1.6;
		font-style: italic;
	}

	.nova-quote-box .author {
  		display: block;
  		margin-top: 1em;
  		font-size: 1.2rem;
  		color: var(--date-color);
	}

	#quote-text,
	#quote-author {
  		opacity: 1;
  		transition: opacity 0.4s ease-in-out;
	}

	.fade-out {
  		opacity: 0;
	}

	.nova-section {
		font-size: 0.95rem;
		text-align: initial;
  		max-width: 700px;
 		margin: 2em auto;
  		padding: 1em;
  		background: var(--section-bg);
  		border-radius: 8px;
  		color: var(--nova-box-text);
  		box-shadow: var(--nova-box-shadow);
		border: 1px solid var(--border);
	}

	.nova-section h2 {
  		font-size: 1.6rem;
  		color: var(--accent);
  		margin-bottom: 1em;
  		text-align: center;
	}

	.nova-section p {
  		line-height: 1.6;
  		margin-bottom: 1em;
	}

	.nova-card {
		position: relative;
		text-align: left;
  		background: var(--card-bg);
		border: 2px solid (--border);
		border-left: 4px solid var(--card-accent);
  		color: var(--info-text);
  		font-family: 'Courier New', monospace;
  		padding: 1em;
  		border-radius: 8px;
 		box-shadow: var(--shadow);
  		flex: 1;
  		font-size: 0.85rem;
		max-width: 55%;
	}

	.nova-card-header {
  		position: absolute;
  		top: -1.2em;
  		left: 0;
  		font-size: 0.75rem;
  		color: var(--card-accent);
  		background: #000;
  		padding: 0.2em 0.5em;
  		border-radius: 4px;
  		box-shadow: 0 0 6px var(--card-accent);
  		font-weight: bold;
	}

	.nova-table {
  		width: 100%;
  		border-collapse: collapse;
  		font-size: 0.85rem;
	}

	.nova-table td {
  		padding: 0.3em 0.4em;
  		border-bottom: 1px dashed #4b0082;
  		vertical-align: top;
	}

	.nova-table td:first-child {
  		color: var(--card-accent);
  		width: 35%;
		padding-right: 0.4em
	}

	.nova-card p {
  		margin: 0.4em 0;
	}

	.nova-img-wrapper {
  		flex: 0 0 auto;
  		max-width: 220px;
	}

	.nova-flex {
  		display: flex;
  		align-items: flex-start;
  		justify-content: space-between;
  		gap: 1em;
 		flex-wrap: nowrap;
	}

	.nova-img {
  		width: 100%;
  		height: auto;
		display: block;
  		border-radius: 8px;
  		box-shadow: 0 0 10px #8a2be2;
  		object-fit: contain;
	}

	.nova-lore,
	.nova-role{
  		font-size: 0.95rem !important;
  		line-height: 1.6;
  		margin-top: 1em;
	}

	@media (max-width: 600px) {
  		.nova-flex {
    		flex-direction: column;
    		align-items: center;
  		}

  		.nova-card,
  		.nova-img {
    		max-width: 100%;
  		}
	}

	.gateway-text {
  		flex: 1 1 55%;
  		font-size: 1rem;
  		line-height: 1.6;
		text-align: left;
	}

	.gateway-flex {
  		display: flex;
  		align-items: center;
  		justify-content: space-between;
  		gap: 2rem;
	}

	.gateway-image {
  		flex: 1 1 40%;
  		max-width: 320px;
	}

	.gateway-image img {
  		width: 100%;
  		height: auto;
  		border-radius: 12px;
  		box-shadow: 0 0 10px var(--card-accent);
  		object-fit: contain;
	}

	@media (max-width: 720px) {
  		.gateway-flex {
    		flex-direction: column;
    		text-align: left;
  		}

  		.gateway-image {
    		margin-top: 1rem;
    		max-width: 100%;
  		}
	}

	.gateway-intro h2 {
  		text-align: center;
  		display: block;
  		width: 100%;
	}

	#comet-trail {
		position: fixed;
		top: 0;
		left: 0;
		pointer-events: none;
		z-index: 9999;
	}

	.comet-particle {
		position: absolute;
		width: 6px;
		height: 6px;
		background: var(--accent);
		border-radius: 50%;
		opacity: 0.9;
		filter: blur(2px);
		transform: translate(-50%, -50%);
		animation: cometFade 0.4s linear forwards;
	}

	@keyframes cometFade {
		0% {
			opacity: 0.9;
			transform: scale(1) translate(-50%, -50%);
		}
		100% {
			opacity: 0;
			transform: scale(0.3) translate(-50%, -50%);
		}
	}
	</style>
</html>